<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"
        "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.chance.dao.StudentDao">
	<select id="queryStudentByid" parameterType="String" resultType="map">
		select S,Sname,Sage,Ssex from student where S=#{id};
	</select>
</mapper>